# Implementation Plans

Generated by the improve skill on 2026-06-20. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Remove unused Python scaffold | P1 | S | — | TODO |
| 002 | Add GitHub Actions CI for config validation | P1 | S | — | TODO |
| 003 | Add config validation script | P1 | S | — | TODO |
| 004 | Add install/deploy script | P2 | S | — | TODO |
| 005 | Remove duplicate MCP servers from opencode config | P2 | S | — | TODO |
| 006 | Add AGENTS.md for AI-agent project context | P1 | S | — | TODO |
| 007 | Design/spike — Build validation + install CLI | P3 | M | 001 (conflict) | TODO |
| 008 | Design/spike — Automated regression testing | P3 | M | 003 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **007 conflicts with 001**: Plan 001 removes the Python scaffold; plan 007 builds a Python CLI. If 001 is executed first, 007 must re-create the Python project structure. If 007 is executed first, 001 should be skipped or revised. **Recommended**: execute 001 first (remove dead weight), then 007 as a design/spike that re-creates the scaffold with purpose.
- **008 depends on 003**: The regression test harness should include structural validation (plan 003) as a prerequisite step. Execute 003 before 008.
- **002 benefits from 003**: The CI workflow can run the validation script from plan 003. Not a hard dependency (the CI can use inline commands), but cleaner if 003 is done first.
- **004 is independent**: The install script doesn't depend on any other plan. Can be executed in any order.
- **005 is independent**: The opencode config cleanup doesn't depend on any other plan.
- **006 is independent**: AGENTS.md doesn't depend on any other plan.

## Recommended execution order

1. **006** — AGENTS.md (quick win, no dependencies, improves all subsequent agent work)
2. **003** — Config validation (foundation for CI and testing)
3. **002** — CI workflow (runs validation on push)
4. **001** — Remove Python scaffold (dead weight cleanup)
5. **004** — Install script (DX improvement)
6. **005** — Clean opencode config (quick cleanup)
7. **008** — Regression testing spike (depends on 003)
8. **007** — CLI tool spike (conflicts with 001; do last after deciding on Python approach)

## Findings considered and rejected

- **Python scaffold as-is**: Keeping the stub `main.py` and empty package is not worth doing — it's dead weight with no purpose. Either remove it (001) or build it into something useful (007).
- **`.pytest_cache/` not in `.gitignore`**: The directory is self-ignoring (pytest creates its own `.gitignore`). Not a real issue.
- **`.opencode/` contains API key**: The directory is gitignored. The key is a Context7 API key that's also in the global config. The fix (005) removes the duplicate MCP section, which eliminates the duplicated key.
